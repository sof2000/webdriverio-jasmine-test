const testData = ["BMW", "Volvo", "Audi"];
testData.forEach( data => {
	console.log(data);
})

const testData = ["BMW", "Volvo", "Audi"];

function myFunction(item) {
	console.log(item);
}

testData.forEach(myFunction);